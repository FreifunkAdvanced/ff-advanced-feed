#!/bin/sh
# post-removal script.

echo "+ removing settings"

rm -f "${IPKG_INSTROOT}/hbbp/p2ptbl/update"

sed \
  -e "/ffrl firewall settings/d" \
  -i "${IPKG_INSTROOT}/etc/firewall.user"
  
sed \
  -e "/gwip gossip/d" \
  -i "${IPKG_INSTROOT}/etc/crontabs/root"

sed \
  -e "/inetable watcher/d" \
  -i "${IPKG_INSTROOT}/etc/crontabs/root"

